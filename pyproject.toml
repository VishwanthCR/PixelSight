[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "pixelsight"
version = "0.1.0"
description = "Multispectral satellite super-resolution and geospatial intelligence"
requires-python = ">=3.12"

[tool.setuptools.packages.find]
where = ["src"]